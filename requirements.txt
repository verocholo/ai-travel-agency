anthropic>=0.40.0
requests>=2.31.0
python-dotenv>=1.0.0
pytest>=8.0.0
# [AGGIUNTI 2026-07-12 — microservizio HTTP per Make.com, vedi service.py]
# flask e gunicorn non erano necessari finché il prototipo era solo un CLI
# (main.py). gunicorn non è installabile in QUESTO sandbox (nessun accesso
# PyPI se non api.anthropic.com — vedi CHANGELOG.md), ma lo sarà sulla
# piattaforma di deploy reale (Render.com), che ha accesso internet pieno.
flask>=3.1.0
gunicorn>=22.0.0
# [AGGIUNTO 2026-08-02 — cartine disegnate in locale, vedi src/map_render.py]
# La cartina del giorno dipendeva da UNA chiamata di rete a Google Static Maps:
# chiave assente, quota esaurita o URL troppo lungo e il cliente riceveva il
# documento SENZA figura — è successo davvero, nel PDF di esempio. Pillow
# disegna lo schema dalle coordinate che abbiamo già pagato, così la cartina
# diventa una proprietà garantita del documento invece di una speranza.
# Il codice degrada comunque se manca (`render_day_map_png()` ritorna None).
Pillow>=10.0.0

# [AGGIUNTO 2026-08-02, RICLASSIFICATO 2026-08-05] pypdf.
#
# Nasceva come dipendenza dei soli test: serviva a guardare dentro il PDF
# prodotto e contare i salti interni che funzionano davvero. La riga qui
# sopra diceva infatti «il prodotto non lo importa da nessuna parte: se
# manca, quel controllo si salta».
#
# DAL 2026-08-05 NON È PIÙ VERO. `src/fascicolo.py` lo importa per cucire le
# guide dentro l'unico file e per allegare il foglio della valigia. Se manca,
# `unisci()` cattura l'eccezione e restituisce il documento principale senza
# capitoli: il cliente riceve un PDF che sembra completo e non lo è, e nessun
# errore compare da nessuna parte.
#
# La nota vecchia è rimasta scritta qui apposta invece di essere cancellata:
# era esatta quando fu scritta, ed è il tipo di frase che fra sei mesi
# convincerebbe qualcuno a togliere questa riga «perché tanto serve solo ai
# test».
pypdf>=3.17

# [AGGIUNTO 2026-08-05 — DIFETTO REALE, trovato al primo deploy dopo quattro
# giorni in cui il prodotto era rotto in produzione senza che nessuno lo
# sapesse.]
#
# `src/checklist_xlsx.py` importa openpyxl per costruire il foglio della
# valigia. Questa riga non c'era. In produzione la libreria non era
# installata, l'import falliva, e `build_checklist_xlsx()` — che cattura ogni
# eccezione apposta per non poter mai togliere il PDF al cliente —
# restituiva `None`.
#
# Effetto: dal giorno in cui il foglio della valigia è stato scritto, NESSUN
# cliente in produzione lo avrebbe ricevuto, e nel PDF il riquadro che lo
# annuncia sarebbe semplicemente sparito. Nessun errore nei log, nessuna
# eccezione, nessun test rosso: il guasto perfetto.
#
# È la stessa forma di tutti i difetti gravi di questo progetto — degrada
# invece di rompersi — ed è il motivo per cui esiste
# `tests/test_dipendenze_dichiarate_2026_08_05.py`, che confronta ogni import
# esterno del prodotto con questo file e fallisce se uno manca.
openpyxl>=3.1
